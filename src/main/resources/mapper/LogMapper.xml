<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yanghuisen.covid19.mapper.LogMapper">
    <insert id="addLog">
        insert into t_log (id,code_str,detail) values (#{id},#{codeStr},#{detail})
    </insert>
</mapper>